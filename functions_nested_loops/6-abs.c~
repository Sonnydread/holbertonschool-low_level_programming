#include "main.h"

/**
 * _abs - Valor absoluto
 * @c: El num
 * Return: valor absoluto del zero
 */

int _abs(int)

{

if (c < 0)
{
int abd_val;

abs_val = c * -1;
return (abs_val);
}
return (c);

}
