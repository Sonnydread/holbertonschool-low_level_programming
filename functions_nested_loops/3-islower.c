#include "main.h"

/**
 * _islower - En minúsculas
 * Cambia de carater
 * Return: 1 para minúscula o 0 para las demás
 */

int _islower(int c)

  
{


if (c >= 97 && c <= 122)
{

return (1);
}
return (0);

}
