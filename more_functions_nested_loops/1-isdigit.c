#include "main.h"

/**
 * _isdigit - Verifica si el carac es un num
 * @x: el num
 * Return: 1 para el dígito  y 0 para otros
 */

int _isdigit(int x)

{

if (x >= 48 && x <= 57)
{
return (1);
}
return (0);
      
}
 
